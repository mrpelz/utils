import { CommonFieldset } from './common.js';
import { main } from './variants/main.js';

export const variants = {
  main,
};

export const Fieldset = {
  ...CommonFieldset,
  variants,
};

export { CommonFieldset } from './common.js';
