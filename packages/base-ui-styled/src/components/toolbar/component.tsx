import { CommonToolbar } from './common.js';
import { main } from './variants/main.js';

export const variants = {
  main,
};

export const Toolbar = {
  ...CommonToolbar,
  variants,
};

export { CommonToolbar } from './common.js';
