import { CommonCheckbox } from './common.js';
import { main } from './variants/main.js';

export const variants = {
  main,
};

export const Checkbox = {
  ...CommonCheckbox,
  variants,
};

export { CommonCheckbox } from './common.js';
